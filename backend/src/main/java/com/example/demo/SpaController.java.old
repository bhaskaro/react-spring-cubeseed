// src/main/java/com/example/demo/SpaController.java
package com.example.demo;

import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.GetMapping;

@Controller
public class SpaController {

  // Catch-all for non-API routes and forward to the SPA index.html.
  // PathPattern in Spring 6 supports "{*path}" for the remainder.
  @GetMapping({"/", "/{*path}"})
  public String index() {
    return "forward:/index.html";
  }
}
